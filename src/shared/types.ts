// PART THAT NEEDS ADDED TO GYM APP
export enum SelectedPage {
  Landing = "landing",
  Projects = "projects",
  Contact = "contact",
}

// activeMenu.ts or in your types file
export enum ActiveMenu {
  About = "ABOUT",
  Projects = "PROJECTS",
  Contact = "CONTACT",
}
